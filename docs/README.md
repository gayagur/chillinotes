# Docs

Diagrams referenced by the main README:

- `architecture.png` — system architecture (Next.js app · Supabase · GCS · Cloud Run worker · Vertex AI)
- `pipeline.png` — the generation pipeline (upload → transcription → Gemini → notebook + study tools)

`screenshots/` — optional extra images for docs (the main app screenshots live in the top-level `/screenshots`).
